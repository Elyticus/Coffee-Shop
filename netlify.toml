# Build the Vite app and publish the compiled output, not the raw source
# (serving /src/main.tsx directly fails with a MIME type error).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
