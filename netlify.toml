# Static site: no build step, publish the repo root as-is.
[build]
  publish = "."

# There is no index.html, so serve home.html at the root URL.
[[redirects]]
  from = "/"
  to = "/home.html"
  status = 200
